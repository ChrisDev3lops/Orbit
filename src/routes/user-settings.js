const express = require('express');
const router = express.Router();

router.post('/api/v1/user/setting', (req, res) => {
    res.json([]);
});

router.get('/api/v1/user/setting', (req, res) => {
    res.json([]);
});

module.exports = router;
